import { FormatFormaPagamentoPipe } from './format-forma-pagamento.pipe';

describe('FormatFormaPagamentoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatFormaPagamentoPipe();
    expect(pipe).toBeTruthy();
  });
});
